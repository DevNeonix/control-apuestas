import { ElementRenderedDirective } from './element-rendered.directive';

describe('ElementRenderedDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementRenderedDirective();
    expect(directive).toBeTruthy();
  });
});
